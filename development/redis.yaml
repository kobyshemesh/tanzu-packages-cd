apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: redis
  namespace: demo
spec:
  chart:
    spec:
      chart: redis
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: bitnami
      version: 18.6.1
  install:
    createNamespace: true
  interval: 10m0s
  serviceAccountName: redis-instance-sa
  targetNamespace: demo
  valuesFrom:
  - kind: Secret
    name: redis-secret
    valuesKey: inline-configurations
